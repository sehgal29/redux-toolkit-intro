# redux-toolkit-intro
This is a basic React counter application using Redux Toolkit for state management. It demonstrates core Redux concepts like slices, actions, reducers, and the store. The UI allows users to increment, decrement, or reset the count, providing a clean and responsive interface styled with CSS.
